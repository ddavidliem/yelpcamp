<nav class="navbar navbar-expand-lg sticky-top bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/campgrounds">Campground</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
            <div class="navbar-nav">
                <a href="/campgrounds/form" class="nav-link active">Add New Campground</a>
            </div>
            <div class="navbar-nav ml-auto">
                <% if(!currentUser){ %>
                    <a href="/auth/register-form" class="nav-link">Register</a>
                    <a href="/auth/login-form" class="nav-link">Login</a>
                    <% } else{ %>
                        <a href="/auth/logout" class="nav-link">Logout</a>
                        <% } %>
            </div>
        </div>
    </div>
</nav>